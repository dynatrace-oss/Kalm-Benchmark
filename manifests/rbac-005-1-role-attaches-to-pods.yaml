apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  annotations:
    ccss_score: "6.0"
    ccss_severity: Medium
    check_path: ClusterRole.rules[].verbs|ClusterRole.rules[].resources|Role.rules[].verbs|Role.rules[].resources|.rules[].verbs|.rules[].resources|.rules[].resources.pods.attach
    description: Allowing roles to attach to pods can be dangerous
    expected: alert
    standards: "[]"
  labels:
    app.kubernetes.io/part-of: kalm-benchmark
    check: RBAC-005-1
  name: rbac-005-1-role-attaches-to-pods
  namespace: kalm-benchmark
rules:
  - apiGroups:
      - ""
    resources:
      - pods/attach
    verbs:
      - create
